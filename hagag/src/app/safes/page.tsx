import { AppLayout } from '@/components/app-layout'
import { SafesContent } from '@/components/safes-content'

export default function SafesPage() {
  return (
    <AppLayout>
      <SafesContent />
    </AppLayout>
  )
}